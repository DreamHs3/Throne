# Handoff — незавершённый раунд remediation PC-100 + возврат к старым замечаниям

Дата: 2026-09-12. Составлено перед передачей работы другому агенту.

## Точка передачи

- Branch: `agent/night-stage1-service-spike`, HEAD **`041b0978`**
  (`fix(PC-100): restrict privileged service RPC surface`).
- Рабочее дерево (НЕЗакоммиченное, это продолжение текущей работы):
  - `M core/server/service_windows.go` (+240/−32) — готовый код, vet/build зелёные;
  - `?? core/server/service_config_policy.go` — НОВЫЙ файл, готов, vet/build зелёные;
  - `M core/server/gen/libcore.proto` (+23 строки, RequestEnvelope/ResponseEnvelope) —
    **PC-110 WIP владельца. Хранить, не коммитить, не переписывать.**
- Проверено на текущем дереве: `gofmt` чисто, `go vet` (CI tags) exit 0,
  `go build` (CI tags) exit 0.

## Что уже сделано в этом раунде (в дереве, ждёт тестов и коммита)

1. **PC-100 config policy** — новый `core/server/service_config_policy.go`:
   - `applyServiceConfigPolicy(coreConfigJSON, serviceDataDir) (string, error)`:
     нормализация `experimental.cache_file.path` → `<dataDir>/cache.db`
     (fail-closed: enabled cache_file без `THRONE_SERVICE_DATA_DIR` → typed
     `ERR_CONFIG_POLICY`), удаление `experimental.clash_api.external_ui*`;
     затем deny-by-default скан: keys `output|path|paths|certificate_path|
     key_path|private_key_path|certificateFile|keyFile|external_ui*`,
     string-префиксы `unix://`, `\\.\pipe`, `\\.\`, `\\?\`; Xray `log.access/
     error` только `none`/пусто. Исключение скана: только
     `$.experimental.cache_file.path` (наш переписанный путь).
   - `validateServiceXrayConfigPolicy` для `xray_config` и `xray_full_configs`.
   - Проверено по закреплённому sing-box `option/*.go`: перечисленные ключи
     покрывают все найденные path-поля (ssh.go:11, rule_set.go:178,
     certificate.go:15, experimental.go:14/24-26, options.go:72, openvpn.go,
     openconnect.go:90, dns.go:180/206).
2. **service_windows.go**: `ServiceStart`/`ServiceCheckConfig` прогоняют
   политику (CheckConfig — тот же контракт, что Start); SDDL-hardening
   (`safeServiceSDDL`: требует `D:P`, запрещает ACE-трастов WD/AN/AU/BU;
   `listenServicePipe` fail-closed); SID-граница: `serveServiceListener`
   (accept-loop) → `authorizeServiceClient` (hook для тестов) →
   `clientTokenIdentity` (Fd() → GetNamedPipeClientProcessId → OpenProcess →
   токен → вручную GetTokenInformation/Tokenuser/Tokengroups через
   `tokenInformation()` + unsafe) → `serviceClientAllowed` (ADR-001:
   `THRONE_SERVICE_ALLOWED_SIDS` + Administrators `S-1-5-32-544`).
   Убедиться, что winio accept-conn реализует `Fd()` — подтверждено
   (file.go:295 `func (f *win32File) Fd()`).

## ЧТО ОСТАЛОСЬ СДЕЛАТЬ (по порядку)

### 1. Тесты PC-100 — новый файл `core/server/service_windows_policy_test.go`

Писать ОТДЕЛЬНЫМ файлом через Write (heredoc python ломается на таком
объёме). Содержание (всё через wire framing после Hello, где указано):

- `TestServiceConfigPolicyRejections` — таблица: log.output абсолют;
  cache_file disabled + client path (нормализация удаляет path — ожидание:
  ошибки НЕТ, path в выводе отсутствует — решающий тест, что «drop» работает);
  tls certificate_path/key_path; ssh private_key_path; local rule-set
  `route.rule_set[].path` и `paths`; predefined dns rule-set path; clash_api
  external_ui/external_ui_download_url; listen `unix://…`; listen `\\.\pipe…`
  → все `ERR_CONFIG_POLICY`. (Прямые вызовы applyServiceConfigPolicy.)
- `TestServiceConfigPolicyNormalization` — cache_file.enabled=true +
  `t.Setenv("THRONE_SERVICE_DATA_DIR", dir)` → нормализованный path ==
  filepath.Join(dir,"cache.db"); external_ui* удалены; без data dir →
  ERR_CONFIG_POLICY.
- `TestServiceXrayConfigPolicy` — log.access/error file → reject; `none` → ok;
  certificateFile/keyFile → reject.
- `TestServiceWirePolicyEndToEnd` (УБИЙЦА обходов) — wire: Start с
  cache_file.enabled=true и evil-path-ами: `C:\Windows\Temp\evil.db`,
  `..\..\Windows\Temp\evil.db`, реальный junction (`cmd /c mklink /J` через
  os/exec, best-effort) → Start принимается, `cache.db` появляется ТОЛЬКО в
  data dir, в ней нет посторонних файлов; Stop. Плюс wire-отклонения
  ERR_CONFIG_POLICY через Start и CheckConfig на `{"log":{"output":…}}`.
- `TestServiceSDDLValidation` — safe: дефолт, `D:P(A;;GA;;;S-1-5-21-…)`;
  unsafe: WD/AN/AU/BU, `D:` без P, без DACL.
- `TestServiceClientAllowedPolicy` — env `THRONE_SERVICE_ALLOWED_SIDS`
  (t.Setenv): owner SID разрешён, второй SID разрешён, admin-группа
  разрешена, чужой SID без admin запрещён, пустой запрещён.
- `TestServiceClientIdentityRealPipe` — РЕАЛЬНЫЙ pipe
  (`THRONE_SERVICE_PIPE=\\.\pipe\ProxyCoreServiceTest-Identity`):
  serveServiceListener + winio.DialPipe из этого же процесса →
  клиент обслуживается (identity = own SID); собств. SID получить через
  windows.OpenProcessToken(windows.CurrentProcess()) + tokenInformation.
  ВАЖНО: ожидание served/denied делать адаптивным:
  `serviceClientAllowed(selfSID, selfGroups)` (если раннер elevated —
  admin-группа всё равно пускает).
- Затем: `gofmt`, `go vet` (CI tags), `go build`, `go test -count=20` ВСЕХ
  PC-100 тестов, regression `go test . ./internal/xray/ ./internal/xraydns/`,
  `git diff --check`.

### 2. PC-010 (два замечания reviewer'а)

- `include/proxycore/storage/ThroneMigration.h` + `.cpp`:
  `DefaultThroneDataDir()` → вернуть `…/Throne/config` (данные Throne лежат в
  config-подкаталоге), обновить комментарий и
  `TestCoexistence` (endsWith("Throne/config")).
- Согласованный SQLite snapshot вместо копии живых файлов:
  в `copyIntoStaging` — `*.db-wal`/`*.db-shm` пропускать; для каждого
  `*.db`: plain-copy `X.db` → `staging/X.db.raw` (+ `-wal`/`-shm`, если
  есть) → `SQLite::Database raw(staging/X.db.raw, OPEN_READWRITE)` (WAL
  recovery в OUR копии) → `SQLite::Database snap(staging/X.db,
  OPEN_READWRITE|CREATE)` → `SQLite::Backup backup(snap, raw);
  backup.executeStep(-1);` (прецедент: src/database/Database.cpp:317
  «Full snapshot first (WAL-safe, unlike a file copy)») → удалить raw-файлы.
  try/catch SQLite::Exception → typed error. Тесты обновить:
  `makeThroneSource` создаёт НАСТОЯЩУЮ sqlite-базу (SQLiteCpp), WAL-тест
  (писатель с открытой транзакцией/открытым соединением, строка живёт в
  -wal → миграция → строка читается из destination; -wal/-shm файлы в
  target отсутствуют). В `tests/proxycore/CMakeLists.txt` добавить в
  `proxycore_tests` исходники SQLiteCpp (Backup/Column/Database/Exception/
  Savepoint/Statement/Transaction.cpp + sqlite3.c) и include-путь
  `3rdparty/SQLiteCpp/include` (в characterization-цели уже есть).
- Отдельный commit: `fix(PC-010): migrate Throne config via consistent SQLite snapshot`.

### 3. PC-020 (два замечания reviewer'а)

- `script/build_go.sh:11-15` — удалить весь блок скачивания updater
  (`curl … throneproj/updater/releases/latest/download/…`).
- `script/windows_installer.iss:69` — удалить секцию `[UninstallDelete]`
  (`{app}\updater.old`) — ProxyCore updater больше не поставляет.
- `tests/proxycore/check_no_updater.sh` — расширить: сканировать также
  `script/` и `.github/workflows/` (запретить слово `updater` целиком в этих
  каталогах; в src/include паттерны прежние).
- Отдельный commit: `fix(PC-020): stop fetching upstream updater in build and packaging`.

### 4. winipcfg vet

- `core/server/internal/boxdns/winipcfg/winipcfg_test.go`: `%w` → `%v`
  ТОЛЬКО внутри t.Errorf/t.Fatalf (vet-строки: ~477, 562, 571, 581, 586, 594,
  614, 624, 629, 637, 657 — проверить все `grep -n '%w'`). fmt.Errorf с `%w`
  не трогать. После: `go vet ./...` чисто. Машинно-зависимые тесты
  (TestSetDNS и др.) всё равно требуют VM — это остаётся BLOCKED.
- Отдельный commit: `fix(tests): make winipcfg tests vet-clean`.

### 5. Документы и финал

- `docs/night-run/pc-100-report.md`: секции «config filesystem policy» и
  «Windows identity boundary»; статус ОСТАЁТСЯ **BLOCKED** до VM-прогонов
  (SCM, non-elevated user test, SDDL-refusal из-под обычного пользователя —
  только VM; сам SID-код и real-pipe identity тест выполняются на этой
  машине).
- `docs/adr/ADR-002-service-host-spike.md` — аддендум №2 (policy + identity).
- `docs/night-run/status.md` — журнал раунда.
- Финальный отчёт с перечнем: что доказано кодом/тестами, что BLOCKED-VM.

## Жёсткие ограничения (не изменены)

- `core/server/gen/libcore.proto` (PC-110 WIP) — НЕ коммитить ни в один из
  четырёх коммитов; после коммитов показать `git show --stat` каждого + что
  WIP остаётся в дереве.
- Не трогать: `server.go`, `dispatch.go`, `parentcheck/*`, `ipc/*`,
  `generate.cpp`, `RouteRule*`, TUN/DNS/routing, sing-box/Xray логику;
  legacy child mode байт-в-байт; SDDL не ослаблять; PC-110/120/130 не
  начинать; Gate 0/1 не объявлять закрытыми; PC-100 = BLOCKED до VM.
- Без push/PR/tag/release; без установки службы; без sc.exe/netsh/реестра;
  не запускать winipcfg сетевые тесты на рабочей машине.
- Инструментарий: Go 1.27.0 + protoc 31.1 портативно в `D:\GLM_project\tools`
  (PATH/GOPATH/GOMODCACHE — см. docs/night-run/morning-review.md).
- Commits: user `ProxyCore Agent <agent@proxycore.local>`.
